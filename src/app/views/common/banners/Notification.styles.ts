import { ITheme } from '@fluentui/react';

export const NotificationStyles = (theme: ITheme) => {
    return {
        container: {
            backgroundColor: 'blue',
            textColor: '#ffffff'
        }
    }
}